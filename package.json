{
  "name": "makakatrade",
  "version": "1.0.0",
  "description": "Modern trading platform - Docker Compose startup scripts",
  "scripts": {
    "dev": "docker compose up --build",
    "up": "docker compose up --build -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f"
  }
}
